{
  "framework": "vite",
  "buildCommand": "BASE_PATH=/ pnpm --filter design-studio build",
  "outputDirectory": "artifacts/design-studio/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
