# ─────────────────────────────────────────────────────────────────
# DESIGN STUDIO — Environment Configuration
# ─────────────────────────────────────────────────────────────────

# ── AI Provider Configuration ──────────────────────────────────────
# Default: Gemini Web2API (local proxy at localhost:8081)
LLM_BASE_URL=http://localhost:8081/v1
LLM_API_KEY=gemini-web2api
LLM_MODEL=gemini-3.5-flash

# ── OpenRouter Fallback (optional) ────────────────────────────────
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_FALLBACK_MODEL=openrouter/free

# ── Production (Cloudflare Tunnel) ───────────────────────────────
# Expose gemini-web2api via Cloudflare Tunnel for production deployment:
#   cloudflared tunnel run <your-tunnel-name>
# Then set:
# LLM_BASE_URL=https://<your-tunnel>.trycloudflare.com/v1

# ── AI Integrations (if using Vercel AI SDK) ─────────────────────
# AI_INTEGRATIONS_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# AI_INTEGRATIONS_OPENROUTER_API_KEY=sk-or-...

# ── Vercel Deployment ────────────────────────────────────────────
# Set these in Vercel dashboard:
#   LLM_BASE_URL       → https://<your-tunnel>.trycloudflare.com/v1
#   OPENROUTER_API_KEY → sk-or-... (optional fallback)