kind = "web"
previewPath = "/"
title = "DESIGN.Studio"
version = "1.0.0"
id = "artifacts/design-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19028

[services.development]
run = "pnpm --filter @workspace/design-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/design-studio", "run", "build" ]
publicDir = "artifacts/design-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19028"
BASE_PATH = "/"
